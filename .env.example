# ==============================================================================
# KRIYA Environment Configuration Template
# ==============================================================================
# Copy this file to .env and configure your network and service endpoints.
# All hosts, ports, and URLs are sourced strictly from environment variables.
# Ports 8000 and 8080 are reserved for external model gateways / inference nodes.
# ==============================================================================

# Sovereign LLM Load Balancer / Phone Gateway (OpenAI-compatible server)
MODEL_BALANCER_URL=http://100.98.154.51:8000/v1
MODEL_BALANCER_KEY=sih2026

# Backend Server Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=5000
BACKEND_URL=http://100.100.63.50:5000

# Frontend Server Configuration
FRONTEND_HOST=0.0.0.0
FRONTEND_PORT=3000
VITE_BACKEND_URL=http://100.100.63.50:5000

# PostgreSQL Database Configuration
DATABASE_URL=postgresql://postgres@100.100.63.50:5432/kriya_db





#   #### 2. Real Gmail SMTP Integration (Optional)
  
#   If you want actual emails sent to your Gmail inbox (vivekananda2201s7@gmail.com), we have also implemented real SMTP delivery. You only need to add your Gmail App  
#   Password to .env:
  
#     # Add to .env for real email delivery:
#     SMTP_HOST=smtp.gmail.com
#     SMTP_PORT=587
#     SMTP_USER=your_email@gmail.com
#     SMTP_PASSWORD=your_16_digit_app_password
#     SMTP_FROM="KRIYA Sovereign AI <your_email@gmail.com>"
  
#   Whenever these settings are present, the backend sends a real email. When running air-gapped without internet, it automatically falls back to the on-screen         
#   simulated code and console logging.
